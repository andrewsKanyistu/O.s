/* definire la struttura che rappresenta il messaggio 
	 i campi devono essere:
	 un intero mtype per indicare il tipo del messaggio
	 un vettore di caratteri per il nome
	 un vettore di caratteri per il cognome
	 un intero per l'eta'
	 un vettore di caratteri per l'email
*/

typedef struct record_anag {

	/* inserire qui i campi */
			
} message;
